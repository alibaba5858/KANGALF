{
  "entities": {
    "User": {
      "title": "User",
      "description": "Player data including balance, factories, and warehouse items.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "Unique identifier for the user." },
        "displayName": { "type": "string", "description": "Display name of the player." },
        "balance": { "type": "number", "description": "Current money of the player." },
        "factories": { "type": "object", "description": "Map of factory IDs to counts." },
        "warehouse": { "type": "object", "description": "Map of item IDs to counts." },
        "lastUpdated": { "type": "string", "format": "date-time", "description": "Last time the data was synced." }
      },
      "required": ["uid", "displayName", "balance", "factories", "warehouse"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "Collection of player profiles and game states."
    }
  }
}
